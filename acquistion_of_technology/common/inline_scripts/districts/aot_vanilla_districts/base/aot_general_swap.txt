# This script generates the district used for swapping when using the proper zone
# purely for aesthetics

is_capped_by_modifier = no

zone_slots = {
    slot_empty
}

show_on_uncolonized = {
    always = no
}

potential = {
    always = no
}

triggered_name = {
    trigger = {
        always = yes
    }
    text = $SWAP_NAME$
}

triggered_desc = {
    trigger = {
        always = yes
    }
    text = $SWAP_DESC$
}