$MODE$ = {
    trigger = {
        from = {
            country_uses_bio_ships = no
        }
    }
    $RESOURCE$ = $AMOUNT$
}
$MODE$ = {
    trigger = {
        from = {
            country_uses_bio_ships = yes
        }
    }
    food = $AMOUNT$
    mult = @alloy_to_food_cost_ratio
}