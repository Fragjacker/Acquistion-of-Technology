owner = {
	add_resource = {
		$RESOURCE$ = $AMOUNT$
	}
	every_owned_leader = {
		limit = {
			$LIMIT$
			num_leader_traits = {
				value > 0
				negative = yes    # Tiers don't matter in this check.
			}
		}
		remove_trait = all_negative
	}
}
