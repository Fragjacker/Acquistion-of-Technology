option = {
    name = $NAME$
    custom_tooltip = $CUSTOM_TOOLTIP$
    allow = {
        resource_stockpile_compare = {
            resource = alloys
            value >= @[ 1000 * ( 500 * two_exp$EXPONENT$ ) ]
        }
        resource_stockpile_compare = {
            resource = acot_sr_dark_energy
            value >= @[ 1000 * ( 125 * two_exp$EXPONENT$ ) ]
        }
        resource_stockpile_compare = {
            resource = sr_dark_matter
            value >= @[ 1000 * ( 125 * two_exp$EXPONENT$ ) ]
        }
        resource_stockpile_compare = {
            resource = acot_sr_stellarite
            value >= @[ 1000 * ( 25 * two_exp$EXPONENT$ ) ]
        }
    }
    add_resource = {
        alloys = @[ - 1000 * ( 500 * two_exp$EXPONENT$ ) ]
        acot_sr_dark_energy = @[ - 1000 * ( 125 * two_exp$EXPONENT$ ) ]
        sr_dark_matter = @[ - 1000 * ( 125 * two_exp$EXPONENT$ ) ]
        acot_sr_stellarite = @[ - 1000 * ( 25 * two_exp$EXPONENT$ ) ]
    }
    enable_special_project = {
        name = $SPECIAL_PROJECT_NAME$
        owner = root
    }
    hidden_effect = {
        remove_country_flag = acot_databank_diplomacy_engaged
    }
}
