option = {
    name = $NAME$
    custom_tooltip = $CUSTOM_TOOLTIP$
    allow = {
        resource_stockpile_compare = {
            resource = alloys
            value >= @[ 500000 * two_exp$EXPONENT$ ]
        }
        resource_stockpile_compare = {
            resource = acot_sr_dark_energy
            value >= @[ 125000 * two_exp$EXPONENT$ ]
        }
        resource_stockpile_compare = {
            resource = sr_dark_matter
            value >= @[ 125000 * two_exp$EXPONENT$ ]
        }
        resource_stockpile_compare = {
            resource = acot_sr_stellarite
            value >= @[ 25000 * two_exp$EXPONENT$ ]
        }
    }
    add_resource = {
        alloys = @[ - 500000 * two_exp$EXPONENT$ ]
        acot_sr_dark_energy = @[ - 125000 * two_exp$EXPONENT$ ]
        sr_dark_matter = @[ - 125000 * two_exp$EXPONENT$ ]
        acot_sr_stellarite = @[ - 25000 * two_exp$EXPONENT$ ]
    }
    enable_special_project = {
        name = $SPECIAL_PROJECT_NAME$
        owner = root
    }
    hidden_effect = {
        remove_country_flag = acot_databank_diplomacy_engaged
    }
}
