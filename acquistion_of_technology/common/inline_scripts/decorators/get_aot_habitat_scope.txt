# Serves as a decorator to reduce scripted effect depth (basically inline the code).
solar_system = {
    random_system_planet = {
        limit = {
            is_aot_habitat = yes
        }
        save_event_target_as = system_aot_habitat
    }
}
event_target:system_aot_habitat = {
    $CODE$
}
