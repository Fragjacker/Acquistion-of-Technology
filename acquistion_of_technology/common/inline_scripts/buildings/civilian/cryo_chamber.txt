inline_script = {
    script = shared/normal_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
category = unity
building_sets = {
    government
    urban
    unity
    aot_building_tier_$TIER$
}
potential = {
    owner? = {
        is_machine_empire = yes
        has_technology = tech_acot_ae_ancient_cryo_chamber
    }
}
planet_modifier = {
    planet_housing_add = @[ 4000 * $TIER$ ]
    planet_amenities_no_happiness_add = @[ 7500 * $TIER$ ]
    job_$JOB_MACHINE$_droid_add = @[ 200 * $TIER$ ]
}
ai_estimate_without_unemployment = yes
additional_ai_weight = 1000
ai_resource_production = {
    unity = @[ 100 * two_exp$TIER$ ]
}