inline_script = {
    script = shared/normal_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
category = resource
building_sets = {
    generator
    mining
    farming
    urban
    aot_building_tier_$TIER$
}
country_modifier = {
    country_resource_max_add = @[ 50000 * two_exp$EXPONENT$ ]
}

resources = {
    category = planet_buildings
    cost = {
        alloys = @[ acot_building_basic_alloy_cost * two_exp$EXPONENT$ ]
        sr_dark_matter = @[ 20 * two_exp$EXPONENT$ ]
        acot_sr_dark_energy = @[ 10 * two_exp$EXPONENT$ ]
    }
    cost = {
        trigger = { owner? = { has_technology = tech_dark_matter_power_core_re } }
        aot_sr_runic_power = @[ 25 * two_exp$EXPONENT$ ]
    }
    cost = {
        trigger = { owner? = { has_technology = tech_dark_matter_power_core_se } }
        acot_sr_stellarite = @[ 25 * two_exp$EXPONENT$ ]
    }
    upkeep = {
        trigger = {
            has_modifier = acot_modifier_ascended_city
        }
        sr_dark_matter = 1
        acot_sr_dark_energy = 1
    }
}
ai_estimate_without_unemployment = yes
additional_ai_weight = 100