# Job bonuses for ALPHA Tier
triggered_planet_modifier = {
    potential = {
        exists = planet_owner
        planet_owner = {
            is_regular_empire = yes
        }
    }
    modifier = {
        pop_acot_researcher_bonus_workforce_mult = @[ $BASE_MULT$ * two_exp$EXPONENT$ ]
        job_acot_researcher_add = $BASE_AMOUNT$
    }
}

triggered_planet_modifier = {
    potential = {
        exists = planet_owner
        planet_owner = { is_gestalt = yes }
    }
    modifier = {
        pop_acot_researcher_drone_bonus_workforce_mult = @[ $BASE_MULT$ * two_exp$EXPONENT$ ]
        job_acot_researcher_drone_add = $BASE_AMOUNT$
    }
}