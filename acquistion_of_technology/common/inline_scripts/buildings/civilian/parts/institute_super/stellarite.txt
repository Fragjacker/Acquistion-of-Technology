# Job bonuses for SIGMA Tier
triggered_planet_modifier = {
    potential = {
        owner? = {
            is_regular_empire = yes
        }
    }
    modifier = {
        pop_acot_researcher_bonus_workforce_mult = @[ $BASE_MULT$ * two_exp$EXPONENT$ ]
        pop_phanon_researcher_bonus_workforce_mult = @[ $BASE_MULT$ * two_exp$EXPONENT$ ]
        pop_stellarite_researcher_bonus_workforce_mult = @[ $BASE_MULT$ * two_exp$EXPONENT$ ]
        job_stellarite_researcher_add = $BASE_AMOUNT$
    }
}

triggered_planet_modifier = {
    potential = {
        owner? = { is_gestalt = yes }
    }
    modifier = {
        pop_acot_researcher_drone_bonus_workforce_mult = @[ $BASE_MULT$ * two_exp$EXPONENT$ ]
        pop_phanon_researcher_drone_bonus_workforce_mult = @[ $BASE_MULT$ * two_exp$EXPONENT$ ]
        pop_stellarite_researcher_bonus_workforce_mult = @[ $BASE_MULT$ * two_exp$EXPONENT$ ]
        job_stellarite_researcher_add = $BASE_AMOUNT$ * $TIER$
    }
}