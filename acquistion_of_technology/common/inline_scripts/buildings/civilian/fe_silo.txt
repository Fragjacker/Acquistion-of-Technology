inline_script = {
    script = shared/automated_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    PRODUCES = "$PRODUCES$"
    COST = "$COST$"
    UPKEEP = "$UPKEEP$"
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
category = resource
building_sets = {
    fallen_empire
    generator
    mining
    farming
    aot_building_tier_$TIER$
}
potential = {
    exists = owner
    owner = {
        OR = {
            is_fallen_empire = yes
            has_technology = tech_fe_silo_2
            has_country_flag = fe_silo_1_can_build
        }
    }
    NOR = {
        has_modifier = resort_colony
        has_modifier = slave_colony
        has_modifier = penal_colony
    }
}
triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = {
            is_wilderness_empire = yes
            has_tradition = tr_purity_finish
        }
    }
    planet_building_build_speed_mult = @[ 0.03 * two_exp$EXPONENT$ ]
    planet_decision_enact_speed_mult = @[ 0.03 * two_exp$EXPONENT$ ]
}
destroy_trigger = {
    always = no
}
country_modifier = {
    country_resource_max_add = @[ 100000 * two_exp$EXPONENT$ ]
    country_resource_max_minor_artifacts_add = @[ 1000 * two_exp$EXPONENT$ ]
    country_naval_cap_add = @[ 125 * two_exp$EXPONENT$ ]
}
show_in_tech = tech_fe_silo_2
additional_ai_weight = 1000
ai_estimate_without_unemployment = yes