inline_script = {
    script = shared/normal_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
exempt_from_ai_planet_specialization = no
building_sets = {
    trade
    aot_building_tier_$TIER$
}
potential = {
    NOR = {
        has_modifier = resort_colony
        has_modifier = slave_colony
        has_modifier = penal_colony
    }
    exists = owner
}
destroy_trigger = {
    OR = {
        has_modifier = resort_colony
        has_modifier = slave_colony
        has_modifier = penal_colony
    }
}
planet_modifier = {
    planet_traders_trade_produces_add = @[ 0.25 * two_exp$EXPONENT$ ]
    planet_traders_upkeep_mult = -0.25
}
resources = {
    category = planet_buildings
    cost = {
        alloys = @[ 600 * two_exp$EXPONENT$ ]
        sr_dark_matter = @[ acot_building_basic_dm_cost * two_exp$EXPONENT$ ]
        acot_sr_dark_energy = @[ acot_building_basic_de_cost * two_exp$EXPONENT$ ]
    }
    cost = {
        trigger = { owner? = { has_technology = tech_dark_matter_power_core_re } }
        aot_sr_runic_power = @[ acot_building_basic_dm_cost * two_exp$EXPONENT$ ]
    }
    cost = {
        trigger = { owner? = { has_technology = tech_dark_matter_power_core_se } }
        acot_sr_stellarite = @[ acot_building_basic_dm_cost * two_exp$EXPONENT$ ]
    }
    produces = {
        trigger = {
            has_modifier = acot_modifier_ascended_city
        }
        trade = @[ 20 * two_exp$EXPONENT$ ]
    }
    upkeep = {
        trigger = {
            has_modifier = acot_modifier_ascended_city
        }
        sr_dark_matter = 2
        acot_sr_dark_energy = 2
    }
}
triggered_planet_modifier = {
    potential = {
        owner? = {
            is_regular_empire = yes
        }
    }
    modifier = {
        job_acot_trader_add = @[ 200 * two_exp$EXPONENT$ ]
    }
}
triggered_planet_modifier = {
    potential = {
        owner? = { is_gestalt = yes }
    }
    modifier = {
        job_acot_trader_drone_add = @[ 200 * two_exp$EXPONENT$ ]
    }
}
additional_ai_weight = 800
ai_resource_production = {
    trade = @[ 150 * two_exp$EXPONENT$ ]
}