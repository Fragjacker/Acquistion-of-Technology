inline_script = {
    script = shared/normal_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
building_sets = {
    industrial
    factory
    foundry
    aot_building_tier_$TIER$
}
country_modifier = {
    country_resource_max_alloys_add = @[ 50000 * two_exp$EXPONENT$ ]
    country_resource_max_consumer_goods_add = @[ 50000 * two_exp$EXPONENT$ ]
}

triggered_planet_modifier = {
    potential = {
        owner? = { is_catalytic_empire = no }
    }
    planet_metallurgists_alloys_produces_add = @[ 2 * two_exp$EXPONENT$ ]
    planet_metallurgists_minerals_upkeep_add = @[ 2 * two_exp$EXPONENT$ ]
}

triggered_planet_modifier = {
    potential = {
        owner? = {
            is_catalytic_empire = yes
            is_machine_empire = no
        }
    }
    planet_metallurgists_alloys_produces_add = @[ 2.25 * two_exp$EXPONENT$ ]
    planet_metallurgists_food_upkeep_add = @[ 3 * two_exp$EXPONENT$ ]
}

triggered_planet_modifier = {
    potential = {
        owner? = {
            is_catalytic_empire = yes
            is_machine_empire = yes
        }
    }
    planet_metallurgists_alloys_produces_add = @[ 4 * two_exp$EXPONENT$ ]
    planet_metallurgists_food_upkeep_add = @[ 5 * two_exp$EXPONENT$ ]
}

triggered_planet_modifier = {
    potential = {
        owner? = { country_uses_consumer_goods = yes }
    }
    planet_artisans_consumer_goods_produces_add = @[ 6 * two_exp$EXPONENT$ ]
    planet_artisans_minerals_upkeep_add = @[ 2 * two_exp$EXPONENT$ ]
}

triggered_planet_modifier = {
    potential = {
        owner? = { country_uses_consumer_goods = no }
    }
    planet_metallurgists_alloys_produces_add = @[ 2 * two_exp$EXPONENT$ ]
    planet_metallurgists_minerals_upkeep_add = @[ 2 * two_exp$EXPONENT$ ]
}

triggered_planet_modifier = {
    potential = {
        owner? = { is_regular_empire = yes }
    }
    modifier = {
        job_acot_specialman_add = @[ 100 * two_exp$EXPONENT$ ]
    }
}

triggered_planet_modifier = {
    potential = {
        owner? = { is_gestalt = yes }
    }
    modifier = {
        job_acot_specialdrone_add = @[ 100 * two_exp$EXPONENT$ ]
    }
}

resources = {
    category = planet_buildings
    cost = {
        alloys = @[ acot_building_basic_alloy_cost * two_exp$EXPONENT$ ]
        sr_dark_matter = @[ acot_building_basic_dm_cost * two_exp$EXPONENT$ ]
        acot_sr_dark_energy = @[ acot_building_basic_de_cost * two_exp$EXPONENT$ ]
    }
    cost = {
        trigger = { owner? = { has_technology = tech_dark_matter_power_core_re } }
        aot_sr_runic_power = @[ acot_building_basic_dm_cost * two_exp$EXPONENT$ ]
    }
    cost = {
        trigger = { owner? = { has_technology = tech_dark_matter_power_core_se } }
        acot_sr_stellarite = @[ acot_building_basic_dm_cost * two_exp$EXPONENT$ ]
    }
}