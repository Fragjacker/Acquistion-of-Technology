inline_script = {
    script = shared/normal_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
inline_script = {
    script = buildings/civilian/parts/vats_and_chambers_cost
    EXPONENT = $EXPONENT$
    PLANET_MODIFIER = "$PLANET_MODIFIER$"
    PLANET_UPKEEP = "$PLANET_UPKEEP$"
}
planet_limit = 1
category = government
building_sets = {
    government
    urban
    aot_building_tier_$TIER$
}
planet_modifier = {
    logistic_growth_mult = @[ 0.1 * two_exp$EXPONENT$ ]
    bonus_pop_growth_mult = @[ 0.5 * two_exp$EXPONENT$ ]
}
potential = {
    owner? = {
        is_hive_empire = yes
        has_technology = $PREREQUISITE$
    }
}
destroy_trigger = {
    exists = owner
    OR = {
        owner = { is_hive_empire = no }
        AND = {
            owner = { is_ai = yes }
            free_district_slots = 0
            free_building_slots = 0
            free_housing <= 0
            free_jobs <= 0
        }
    }
}
ai_estimate_without_unemployment = yes
additional_ai_weight = 5000
ai_resource_production = {
    trigger = {
        planet_owner = {
            country_uses_food = yes
        }
    }
    food = @[ 100 * two_exp$EXPONENT$ ]
}
ai_resource_production = {
    unity = @[ 100 * two_exp$EXPONENT$ ]
}