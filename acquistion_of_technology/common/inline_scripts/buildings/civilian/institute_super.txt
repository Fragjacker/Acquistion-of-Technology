inline_script = {
    script = shared/normal_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
can_be_ruined = no
category = research
building_sets = {
    research
    aot_building_tier_$TIER$
}
potential = {
    NOR = {
        has_modifier = resort_colony
        has_modifier = slave_colony
        has_modifier = penal_colony
    }
}

destroy_trigger = {
    OR = {
        has_modifier = resort_colony
        has_modifier = slave_colony
        has_modifier = penal_colony
    }
}

resources = {
    category = planet_buildings
    produces = {
        physics_research = @[ 200 * two_exp$EXPONENT$ ]
        engineering_research = @[ 200 * two_exp$EXPONENT$ ]
        society_research = @[ 200 * two_exp$EXPONENT$ ]
        minor_artifacts = @[ 5 * two_exp$EXPONENT$ ]
        sr_dark_matter = @[ 50 * two_exp$EXPONENT$ ]
        acot_sr_dark_energy = @[ 50 * two_exp$EXPONENT$ ]
    }
    produces = {
        trigger = { planet_owner = { has_technology = tech_dark_matter_power_core_re } }
        aot_sr_runic_power = @[ 50 * two_exp$EXPONENT$ ]
    }
    produces = {
        trigger = { planet_owner = { has_technology = tech_dark_matter_power_core_se } }
        acot_sr_stellarite = @[ 50 * two_exp$EXPONENT$ ]
    }
    cost = {
        alloys = @[ 6000 * two_exp$EXPONENT$ ]
        sr_dark_matter = @[ 6000 * two_exp$EXPONENT$ ]
        acot_sr_dark_energy = @[ 6000 * two_exp$EXPONENT$ ]
    }
    cost = {
        trigger = { planet_owner = { has_technology = tech_dark_matter_power_core_re } }
        aot_sr_runic_power = @[ 6000 * two_exp$EXPONENT$ ]
    }
    cost = {
        trigger = { planet_owner = { has_technology = tech_dark_matter_power_core_se } }
        acot_sr_stellarite = @[ 6000 * two_exp$EXPONENT$ ]
    }
    upkeep = {
        trigger = { planet_owner = { NOT = { has_technology = tech_acot_fractured_autoharvesting } } }
    }
}

country_modifier = {
    country_scientist_cap_add = @[ 1 * $TIER$ ]
    physics_tech_cost_mult = @[ - 0.10 * two_exp$EXPONENT$ ]
    society_tech_cost_mult = @[ - 0.10 * two_exp$EXPONENT$ ]
    engineering_tech_cost_mult = @[ - 0.10 * two_exp$EXPONENT$ ]
    all_technology_research_speed = @[ 0.2 * two_exp$EXPONENT$ ]
}

triggered_planet_modifier = {
    potential = {
        exists = planet_owner
        planet_owner = {
            is_regular_empire = yes
        }
    }
    modifier = {
        job_acot_researcher_add = @[ 2000 * $TIER$ ]
        pop_acot_researcher_bonus_workforce_mult = @[ 1 + ( 0.1 * two_exp$EXPONENT$ ) ]
    }
}

triggered_planet_modifier = {
    potential = {
        exists = planet_owner
        planet_owner = { is_gestalt = yes }
    }
    modifier = {
        job_acot_researcher_drone_add = @[ 2000 * $TIER$ ]
        pop_acot_researcher_drone_bonus_workforce_mult = @[ 1 + ( 0.1 * two_exp$EXPONENT$ ) ]
    }
}