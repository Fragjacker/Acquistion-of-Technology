inline_script = {
    script = shared/normal_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
category = pop_assembly
building_sets = {
    government
    urban
    aot_building_tier_$TIER$
}
potential = {
    owner? = {
        is_hive_empire = yes
        has_technology = $PREREQUISITE$
    }
}
destroy_trigger = {
    owner? = {
        is_hive_empire = no
    }
}
convert_to = {
    building_offspring_nest
}
triggered_planet_modifier = {
    potential = {
        NAND = {
            owner? = {
                has_origin = origin_progenitor_hive
            }
        }
    }
    job_$JOB_HIVE$_add = @[ 100 * two_exp$TIER$ ]
}
triggered_planet_modifier = {
    potential = {
        owner? = {
            has_origin = origin_progenitor_hive
        }
    }
    job_acot_offspring_add = @[ 100 * two_exp$TIER$ ]
}
triggered_planet_modifier = {
    potential = {
        owner? = {
            has_technology = tech_gene_tailoring
        }
        any_owned_pop_group = {
            has_auto_modding_trait = yes
        }
    }
    modifier = {
        auto_mod_monthly_add = @[ 400 * two_exp$TIER$ ]
    }
}
triggered_planet_modifier = {
    potential = {
        owner? = {
            has_origin = origin_overtuned
        }
        any_owned_pop_group = {
            has_auto_modding_trait = yes
        }
    }
    modifier = {
        auto_mod_monthly_add = @[ 400 * two_exp$TIER$ ]
    }
}
triggered_planet_modifier = {
    potential = {
        owner? = { has_active_tradition = tr_cybernetics_synaptic_sub_processing }
    }
    modifier = {
        job_augmentor_drone_add = @[ 200 * two_exp$TIER$ ]
    }
}
resources = {
    category = planet_buildings
    cost = {
        trigger = {
            owner? = {
                is_wilderness_empire = no
            }
        }
        alloys = @[ 250 * two_exp$TIER$ ]
    }
    cost = {
        trigger = {
            owner? = {
                is_wilderness_empire = yes
            }
        }
        alloys = @[ 100 * two_exp$TIER$ ]
        food = @[ 50 * two_exp$TIER$ ]
        biomass = @[ 100 * two_exp$TIER$ ]
    }
    cost = {
        $COST$
    }
    upkeep = {
        trigger = {
            owner? = {
                is_wilderness_empire = no
                NOT = { has_technology = tech_acot_fractured_autoharvesting }
            }
        }
        energy = @[ 1 * two_exp$TIER$ ]
        sr_dark_matter = @[ 2 * two_exp$TIER$ ]
        acot_sr_dark_energy = @[ 2 * two_exp$TIER$ ]
    }
    upkeep = {
        trigger = {
            owner? = {
                is_wilderness_empire = yes
                NOT = { has_technology = tech_acot_fractured_autoharvesting }
            }
        }
        food = @[ 1 * two_exp$TIER$ ]
        sr_dark_matter = @[ 2 * two_exp$TIER$ ]
        acot_sr_dark_energy = @[ 2 * two_exp$TIER$ ]
    }
}
ai_estimate_without_unemployment = yes
additional_ai_weight = 1000
ai_resource_production = {
    unity = @[ 100 * two_exp$TIER$ ]
}