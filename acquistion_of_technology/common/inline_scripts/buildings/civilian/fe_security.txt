inline_script = {
    script = shared/automated_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    PRODUCES = "$PRODUCES$"
    COST = "$COST$"
    UPKEEP = "$UPKEEP$"
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
category = government

building_sets = {
    fallen_empire
    urban
}

potential = {
    exists = owner
    planet_owner = {
        has_technology = $PREREQUISITE$
    }
    NOR = {
        has_modifier = resort_colony
        has_modifier = slave_colony
        has_modifier = penal_colony
    }
}

destroy_trigger = {
    always = no
}

country_modifier = {
    country_edict_fund_add = @[ 10 * two_exp$EXPONENT$ ]
    country_counter_espionage_add = @[ 5 * two_exp$EXPONENT$ ]
    country_counter_espionage_mult = @[ 0.01 * two_exp$EXPONENT$ ]
}

system_modifier = {
    ship_orbit_upkeep_mult = @[ - 0.025 * two_exp$EXPONENT$ ]
}

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = {
            is_regular_empire = yes
        }
    }
    modifier = {
        planet_crime_add = @[ - 40 * two_exp$EXPONENT$ ]
    }
}

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = {
            is_gestalt = yes
        }
    }
    modifier = {
        planet_crime_no_happiness_add = @[ - 40 * two_exp$EXPONENT$ ]
    }
}

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = {
            is_gestalt = no
            is_fallen_empire = yes
        }
    }
    modifier = {
        job_fe_protector_add = @fe_jobs
    }
}

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = {
            is_regular_empire = yes
        }
    }
    modifier = {
        job_enforcer_add = @fe_jobs
    }
}

triggered_desc = {
    trigger = {
        exists = owner
        owner = {
            is_regular_empire = yes
            NOT = { has_valid_civic = civic_civil_education }
        }
    }
    text = job_enforcer_desc
}

triggered_desc = {
    trigger = {
        exists = owner
        owner = { has_valid_civic = civic_civil_education }
    }
    text = job_educator_effect_desc
}

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = {
            is_gestalt = yes
        }
    }
    modifier = {
        job_patrol_drone_add = @fe_jobs
    }
}

triggered_desc = {
    trigger = {
        exists = owner
        owner = {
            is_gestalt = yes
        }
    }
    text = job_patrol_drone_effect_desc
}