inline_script = {
	script = shared/automated_buildings_base
	ALLOW = "$ALLOW$"
	CAN_BUILD = $CAN_BUILD$
	PREREQUISITE = $PREREQUISITE$
	UPGRADES_INTO = $UPGRADES_INTO$
	PRODUCES = "$PRODUCES$"
	COST = "$COST$"
	UPKEEP = "$UPKEEP$"
	AI_FACTORS = "$AI_FACTORS$"
	TIER = $TIER$
}
category = research
potential = {
	exists = planet_owner
	planet_owner = {
		has_technology = tech_fe_master_archive
	}
}
triggered_planet_modifier = {
	potential = {
		exists = planet_owner
		planet_owner = {
			is_fallen_empire = yes
		}
	}
	job_fe_archivist_add = @[ 1000 * $TIER$ ]
}
triggered_planet_modifier = {
	potential = {
		exists = planet_owner
		planet_owner = {
			is_individualistic_empire = yes
		}
	}
	modifier = {
		job_$JOB$_add = @[ fe_jobs * $TIER$ ]
	}
}
triggered_desc = {
	trigger = {
		exists = planet_owner
		planet_owner = {
			is_individualistic_empire = yes
		}
	}
	text = job_head_researcher_effect_desc
}
triggered_planet_modifier = {
	potential = {
		exists = planet_owner
		planet_owner = {
			is_hive_empire = yes
		}
	}
	modifier = {
		job_$JOB_DRONE$_engineer_add = @[ fe2_jobs * $TIER$ ]
		job_$JOB_DRONE$_biologist_add = @[ fe2_jobs * $TIER$ ]
		job_$JOB_DRONE$_physicist_add = @[ fe2_jobs * $TIER$ ]
	}
}
triggered_desc = {
	trigger = {
		exists = planet_owner
		planet_owner = {
			is_hive_empire = yes
		}
	}
	text = job_brain_drone_effect_desc
}
triggered_planet_modifier = {
	potential = {
		exists = planet_owner
		planet_owner = {
			is_machine_empire = yes
		}
	}
	modifier = {
		job_$JOB_MACHINE$_engineer_add = @[ fe2_jobs * $TIER$ ]
		job_$JOB_MACHINE$_biologist_add = @[ fe2_jobs * $TIER$ ]
		job_$JOB_MACHINE$_physicist_add = @[ fe2_jobs * $TIER$ ]
	}
}
triggered_desc = {
	trigger = {
		exists = planet_owner
		planet_owner = {
			is_machine_empire = yes
		}
	}
	text = job_calculator_effect_desc
}