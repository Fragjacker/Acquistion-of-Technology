inline_script = {
    script = shared/automated_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    PRODUCES = "$PRODUCES$"
    COST = "$COST$"
    UPKEEP = "$UPKEEP$"
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
category = manufacturing
building_sets = {
    industrial
    foundry
}
potential = {
    exists = planet_owner
    planet_owner = {
        has_technology = $PREREQUISITE$
    }
    NOR = {
        has_modifier = resort_colony
        has_modifier = slave_colony
        has_modifier = penal_colony
    }
}
planet_modifier = {
    planet_structures_upkeep_mult = @[ - 0.05 * $TIER$ ]
}
triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = {
            merg_is_awakened_fe = yes
            is_fallen_empire_spiritualist = no
        }
    }
    modifier = {
        job_acot_fe_foundry_add = @[ 200 * $TIER$ ]
    }
}
triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = {
            is_regular_empire = yes
        }
        NOT = { has_modifier = penal_colony }
    }
    job_$JOB$_add = @[ fe_jobs * $TIER$ ]
}

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = {
            is_hive_empire = yes
        }
        NOT = { has_modifier = penal_colony }
    }
    job_$JOB_DRONE$_drone_add = @[ fe_jobs * $TIER$ ]
}
triggered_planet_modifier = {
    potential = {
        exists = planet_owner
        planet_owner = {
            is_machine_empire = yes
        }
    }
    modifier = {
        job_$JOB_MACHINE$_add = @[ fe_jobs * $TIER$ ]
    }
}