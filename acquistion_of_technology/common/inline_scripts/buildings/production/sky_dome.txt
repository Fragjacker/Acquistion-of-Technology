inline_script = {
    script = shared/automated_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    PRODUCES = "$PRODUCES$"
    COST = "$COST$"
    UPKEEP = "$UPKEEP$"
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
category = amenity
building_sets = {
    urban
    bio_trophy
}
potential = {
    exists = planet_owner
    planet_owner = {
        is_individualistic_empire = yes
        has_technology = $PREREQUISITE$
    }
    NOR = {
        has_modifier = slave_colony
        has_modifier = penal_colony
    }
}
convert_to = {
    building_drone_megastorage
    building_expanded_warren
    building_communal_housing_large
}
planet_modifier = {
    planet_housing_add = @[ 2500 * $TIER$ ]
    planet_amenities_add = @[ 3750 * $TIER$ ]
    planet_pops_upkeep_mult = @[ - 0.05 * $TIER$ ]
    planet_max_districts_add = @[ 2 * $TIER$ ]
}
triggered_planet_modifier = {
    potential = {
        exists = planet_owner
        planet_owner = {
            has_valid_civic = civic_machine_servitor
        }
    }
    job_bio_trophy_add = @[ 3750 * $TIER$ ]
}