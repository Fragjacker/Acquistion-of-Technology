inline_script = {
    script = shared/automated_buildings_base
    ALLOW = "$ALLOW$"
    CAN_BUILD = $CAN_BUILD$
    PREREQUISITE = $PREREQUISITE$
    UPGRADES_INTO = $UPGRADES_INTO$
    PRODUCES = "$PRODUCES$"
    COST = "$COST$"
    UPKEEP = "$UPKEEP$"
    AI_FACTORS = "$AI_FACTORS$"
    TIER = $TIER$
}
category = unity
building_sets = {
    unity
    aot_building_tier_$TIER$
}
potential = {
    exists = planet_owner
    planet_owner = {
        is_spiritualist = yes
        has_technology = $PREREQUISITE$
    }
}
destroy_trigger = {
    exists = planet_owner
    planet_owner = {
        is_spiritualist = no
    }
}
convert_to = {
    building_fe_administration_2
    building_fe_administration_machine_2
    building_fe_administration_hive_2
}
triggered_planet_modifier = {
    potential = {
        exists = planet_owner
        planet_owner = {
            is_fallen_empire_spiritualist = yes
        }
    }
    job_acot_fe_priest_add = @[ fe_jobs * $TIER$ ]
}
triggered_planet_modifier = {
    potential = {
        exists = planet_owner
        planet_owner = {
            is_fallen_empire = no
            is_spiritualist = yes
        }
    }
    job_$JOB$_add = @[ fe2_jobs * $TIER$ ]
}
triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = { is_fallen_empire = yes }
    }
    job_fe_augur_add = @[ 300 * $TIER$ ]
}

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = { has_tradition = tr_psionics_psi_corps }
    }
    job_telepath_add = @fe_jobs
}

triggered_country_modifier = {
    potential = {
        exists = owner
        owner = {
            has_country_flag = breached_shroud
        }
    }
    shroud_delve_cost = @[ - 0.25 * $TIER$ ]
}

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = { NOT = { has_tradition = tr_psionics_psi_corps } }
    }
    planet_bureaucrats_produces_mult = @[ 0.25 * $TIER$ ]
}
show_tech_unlock_if = {
    OR = {
        is_spiritualist = yes
        has_make_spiritualist_perk = yes
    }
}
