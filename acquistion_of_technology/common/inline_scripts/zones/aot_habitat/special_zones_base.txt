base_buildtime = @zone_buildtime
potential = {
	# planet scope
	is_planet_class = $HABITAT_TYPE$
}
unlock = {
	# planet scope
	always = yes
}
resources = {
	category = planet_zones
	cost = {
		minerals = @[ zone_cost * 0.25 * two_exp$EXPONENT$ ]
	}
}
zone_sets = {
	$ZONE_SETS$
}