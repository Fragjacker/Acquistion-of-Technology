zone_aot_$TIER_NAME$_habitat_special_booster = {
    icon = GFX_colony_type_$ICON_TYPE$
    inline_script = {
        script = zones/aot_habitat/special_zones_base
        EXPONENT = $EXPONENT$
        HABITAT_TYPE = $HABITAT_TYPE$
        SWAP_DISTRICT = $SWAP_DISTRICT$
        ZONE_SETS = "$ZONE_SETS$"
    }
    inline_script = {
        script = zones/aot_habitat/part/planet_modifer_shared
        RESEARCH_MULT = -0.5
        RESOURCE_MULT = -0.5
        INJECTED_MODIFIERS = "$INJECTED_MODIFIERS_POSITIVE$"
    }
    inline_script = {
        script = zones/aot_habitat/zone_job_swaps/$SWAP_MODE$
        AMOUNT = @habitat_zone_district_1_job
        NORMAL_JOB = $NORMAL_JOB$
        GESTALT_JOB = $GESTALT_JOB$
    }
    included_building_sets = {
        industrial
    }
}
zone_aot_$TIER_NAME$_habitat_special_research = {
    icon = GFX_district_specialization_research
    inline_script = {
        script = zones/aot_habitat/special_zones_base
        EXPONENT = $EXPONENT$
        HABITAT_TYPE = $HABITAT_TYPE$
        SWAP_DISTRICT = $SWAP_DISTRICT$
        ZONE_SETS = "$ZONE_SETS$"
    }
    inline_script = {
        script = jobs/zones/aot_researchers
        AMOUNT = @[ habitat_zone_district_1_job * 0.75 * 2 ]
        JOBS = $JOB_TIER$_researcher
        JOBS_GESTALT = $JOB_TIER$_researcherdrone
    }
    inline_script = {
        script = zones/aot_habitat/zone_job_swaps/$SWAP_MODE$
        AMOUNT = @[ habitat_zone_district_1_job * ( 0.25 ) ]
        NORMAL_JOB = $NORMAL_JOB$
        GESTALT_JOB = $GESTALT_JOB$
    }
    inline_script = {
        script = zones/aot_habitat/part/planet_modifer_shared
        RESEARCH_MULT = 1
        RESOURCE_MULT = -0.75
        INJECTED_MODIFIERS = "$INJECTED_MODIFIERS_NEGATIVE$"
    }
    included_building_sets = {
        research
    }
}
zone_aot_$TIER_NAME$_habitat_special_resources = {
    icon = GFX_district_specialization_minerals
    inline_script = {
        script = zones/aot_habitat/special_zones_base
        EXPONENT = $EXPONENT$
        HABITAT_TYPE = $HABITAT_TYPE$
        SWAP_DISTRICT = $SWAP_DISTRICT$
        ZONE_SETS = "$ZONE_SETS$"
    }
    inline_script = {
        script = jobs/zones/aot_miners
        AMOUNT = @[ habitat_zone_district_1_job * 0.8 * 0.75 * 2 ]
        JOBS = $JOB_TIER$_mineralman
        JOBS_GESTALT = $JOB_TIER$_mineraldrone
    }
    inline_script = {
        script = jobs/zones/aot_technicians
        AMOUNT = @[ habitat_zone_district_1_job * 0.2 * 0.75 * 2 ]
        JOBS = $JOB_TIER$_energyman
        JOBS_GESTALT = $JOB_TIER$_energydrone
    }
    inline_script = {
        script = zones/aot_habitat/zone_job_swaps/$SWAP_MODE$
        AMOUNT = @[ habitat_zone_district_1_job * ( 0.25 ) ]
        NORMAL_JOB = $NORMAL_JOB$
        GESTALT_JOB = $GESTALT_JOB$
    }
    inline_script = {
        script = zones/aot_habitat/part/planet_modifer_shared
        RESEARCH_MULT = -0.75
        RESOURCE_MULT = 1
        INJECTED_MODIFIERS = "$INJECTED_MODIFIERS_NEGATIVE$"
    }
    included_building_sets = {
        mining
        energy
    }
}