base_buildtime = @zone_buildtime

potential = {
    inline_script = {
        script = shared/aot_zone_possible_trigger
        OWNER_REQ = "$OWNER_REQ$"
        DESTROY_REQ = "$DESTROY_REQ$"
        TIER_TYPE = delta
        NEXT_TIER_TYPE = alpha
    }
    $PLANET_REQ$
}

unlock = {
    # planet scope
    hidden_trigger = {
        exists = owner
        owner = {
            is_wilderness_empire = no
        }
    }
}

resources = {
    category = planet_zones
    cost = {
        minerals = @zone_cost
        sr_dark_matter = @aot_zone_DM_darkmatter_cost
    }
}

planet_modifier = {
    zone_building_slots_add = 3
}