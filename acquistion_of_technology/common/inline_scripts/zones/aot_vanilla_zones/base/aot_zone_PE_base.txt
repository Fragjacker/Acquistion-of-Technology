base_buildtime = @zone_buildtime

potential = {
    inline_script = {
        script = shared/aot_zone_possible_trigger
        OWNER_REQ = "$OWNER_REQ$"
        DESTROY_REQ = "$DESTROY_REQ$"
        TIER_TYPE = phanon
        NEXT_TIER_TYPE = sigma
    }
    $PLANET_REQ$
}

unlock = {
    # planet scope
    hidden_trigger = {
        exists = owner
        owner = {
            is_wilderness_empire = no
        }
    }
}

resources = {
    category = planet_zones
    cost = {
        minerals = @zone_cost
        aot_sr_runic_power = @aot_zone_PE_runic_cost
    }
}

planet_modifier = {
    zone_building_slots_add = 3
}