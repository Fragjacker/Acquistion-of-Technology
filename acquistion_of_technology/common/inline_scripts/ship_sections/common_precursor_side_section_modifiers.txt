# This contains a generic version of section modifiers for any type of precursor ship.
modifier = {
    ship_hull_add = @[ 10000 * two_exp$EXPONENT$ / 2 ]
    ship_hull_regen_add_static = @[ 1000 * two_exp$EXPONENT$ / 2 ]
    ship_shield_add = @[ 2000 * two_exp$EXPONENT$ ]
    ship_shield_regen_add_static = @[ 200 * two_exp$EXPONENT$ / 2 ]
    ship_armor_add = @[ 2000 * two_exp$EXPONENT$ ]
    ship_armor_regen_add_static = @[ 100 * two_exp$EXPONENT$ / 2 ]
}